<h1>Products#index</h1>

<%= link_to "New Product", new_exempla_product_path if authenticated? %>

<div id="products">
  <% @products.each do |p| %>
    <div>
      <%= link_to p.name, p %>
    </div>
  <% end %>
</div>